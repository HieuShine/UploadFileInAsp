@model IEnumerable<uploadFile.Models.doanhngiep>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tailieudinhkem.DuongDan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tenDN)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tailieudinhkem.DuongDan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tenDN)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idDN }) |
            @Html.ActionLink("Details", "Details", new { id=item.idDN }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idDN })
        </td>
    </tr>
}

</table>
